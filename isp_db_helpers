import sqlite3


def getInvoiceNumsIDs(cur):

  invoiceIDnumsSQL = "SELECT invoice_id invoice_num from INVOICES"

  cur.execute(invoiceIDnumsSQL)

  invoiceNumsIDs = cur.fetchall()

  return [invoice[0] for invoice in invoiceNumsIDs]